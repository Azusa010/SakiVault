{
  "buildCommand": "npm run build-only",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [
    {
      "source": "/api/proxy/(.*)",
      "destination": "/api/proxy/[...path].ts"
    }
  ]
}
